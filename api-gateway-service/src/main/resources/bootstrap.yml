spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://configserver:8888
encrypt:
  failOnError: false
  
eureka:
  instance:
    hostname: gateway
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/  